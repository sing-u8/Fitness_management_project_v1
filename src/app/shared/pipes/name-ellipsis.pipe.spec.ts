import { NameEllipsisPipe } from './name-ellipsis.pipe';

describe('NameEllipsisPipe', () => {
  it('create an instance', () => {
    const pipe = new NameEllipsisPipe();
    expect(pipe).toBeTruthy();
  });
});
